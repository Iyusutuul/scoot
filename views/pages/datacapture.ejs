<!DOCTYPE html>
<html lang="en">
    <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width", initial-scale="1.0">
    <title>Crata - Data Capture</title>
    <link rel="stylesheet" href="styles/dashboard.css" type="text/css" media="all">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/semantic-ui/2.2.4/semantic.min.css">
    <script src="https://cdn.jsdelivr.net/semantic-ui/2.2.4/semantic.min.js"></script>
    <script src="https://kit.fontawesome.com/6bb208eccf.js" 
    crossorigin="anonymous"></script>
</head>
<style>
    .error { color: red; }
    div.preview-images > img {
    width: 70%;
    height: 60%;
    margin-top: -450px;
    margin-left: 450px;
    position: relative;
    }
    .button {display: inline-block;
    padding: 10px 10px;
    color: white;
    text-align: center;
    text-decoration: none;
    border-radius: 5px;
    transition: transform 0.2s ease;
}
    .button:hover {
    transform: scale(1.1);
    
    }
    .fa-solid{
    float: right;
    line-height: 1rem;
    }
       
    .ui.sidebar {
   z-index: 1000; /* Ensure sidebar is above content */
height: calc(100% - 50px); /* Make sidebar height fill the remaining space */
width: 200px !important;
flex-direction: center;
padding-top: 45px;
align-items: center;
justify-content: center;
}
  
.profile-pic {
    display: block;
    border-radius: 50%;
    width: 60px;  /* Adjust size as needed */
    height: 60px; /* Adjust size as needed */
}
.profile-img {
  margin: 0em auto;
}
.pusher {
    z-index: 1000; /* Ensure pusher content stays below the sidebar */
    }
.profile_wrapper{
    display: inline-flex; /* Use flexbox */
    justify-content: flex-start; /* Center horizontally */
    align-items: flex-start; /* Center vertically */
}
.ui.sidebar.item {
            display: flex;
            /* Use flexbox for better alignment */
            padding: 10px;
            /* Adjust padding as necessary */
            align-items: center;
            /* Center items vertically */
            color: black;
            
        }
        .ui.sidebar.item:hover {
            color: #fff;
            
        }
.profile-img {
  display: block;
  margin: auto;
}
.profile-container {
    display: flex;
    text-align: center; /* Center text inside the container */
    margin-top: 60px;
    margin-left: 30px;
}
    </style>
<body>
    <div id="header">
        <div style="background-color: #007bff;" class="ui borderless fixed menu">
            <a style="font-size: 15px; font-weight: bold; color: white;" class="item">Crata</a>
            <a style="color: white;border: none;" class="item" id="menu-toggle">
            <i class="bars icon"></i></a>
            <a href="/dashboard" style="font-size: 15px; font-weight: bold; color: white;" 
            class="item"><i class="fa-solid fa-house"></i></a>
        </div>
    </div>
    <div style="background-color: gainsboro;margin: 0px auto;" class="ui sidebar vertical menu">
        <div class="item">
            <img src="<%= profilePicUrl %>" class="ui centered circular image profile-img" alt="Profile Picture" 
            onerror="this.onerror=null;this.src='default-profile.png';" 
            style=" height: 50px;margin-bottom: 5px;;">
            <!-- <h2 style="text-align: center;font-size: 15px;opacity: 0.5;">Welcome back</h2> -->
            <h2 style="text-align: center;font-size: 15px;margin-top: 1px;margin-bottom: 10px;opacity: .5;"></h2>
             <!-- <span><%= firstname %></span> -->
         </div> 
        <a class="item">Roadmap<i class="fa-solid fa-map"></i></a>
        <a href="/viewpending" class="item"><i class="fa-solid fa-hourglass-start"></i>Pending</a>
        <a href="/capture" class="item"><i class="fa-solid fa-camera"></i> Data Capture</a>
        <a href="/visits" class="item"><i class="fa-solid fa-person-walking-luggage"></i>Visitation</a>
        <a class="item"><i class="fa-solid fa-wallet"></i>Wallet</a>
        <a href="/profile" class="item"><i class="fa-solid fa-user"></i>Profile</a>
        <a href="/logout" class="item"><i class="fa-solid fa-right-from-bracket"></i>Logout</a>
    </div>

    <div class="pusher">
        <!-- <div class="profile_wrapper">
            <div class="profile-container">
                <img src="<%= profilePicUrl %>" class="profile-pic" alt="Profile Picture"
                     onerror="this.onerror=null;this.src='default-profile.png';">
            </div>
            <div class="ui container">
                <p style="margin-top: 80px; padding-left: 15px;
                font-size: 15px; color: orange;">
                    Welcome <%= firstname %>!
            </p>
            </div>
        </div> -->
    </div>
    <script>
        $(document).ready(function() {
            $('#menu-toggle').click(function() {
                $('.ui.sidebar').sidebar('toggle');
            });
        });
    </script>
    </div>
</div>
<div class="pusher">
    <form id="uploadForm" action="/capture" method="POST">    
        <div class="loginform">       
            <% if (msg) { %>
            <div class="alert_success"><%= msg %></div>
        <% } %>             
        <div class="inputs">
            <div>
                <label for="app_type">
                    <input style="margin-top: -50px;" type="text"
                        id="app_type" name="application_type" required="" 
                            maxlength="6" placeholder="Application Type" >
                                </label>
                            </div>
                        </div>
                    <div>
                <label for="term_type">
            <input style="margin-top: 0px;" type="text" id="term_type" 
        name="term_type" required="" minlength="7" maxlength="11" placeholder="Terminal Type" >
    </label>
        </div>
            <div>
                <label for="term_serial">
                    <input style="margin-top: 0px;" type="text"
                        id="term_serial" name="term_serial"  maxlength="11" placeholder="Terminal Serial" 
                            required>
                                </label>
                            </div>
                        <div>
                    <label for="sim_type">
                <input style="margin-top: 0px;" type="text" id="sim_type" 
            name="sim_type" required="" maxlength="3" placeholder="Sim Type" >
        </label>
    </div>
        <div>
            <label for="sim_serial">
                <input style="margin-top: 0px;" type="text"
                    id="sim_serial" name="sim_serial" maxlength="11"
                     placeholder="Sim Serial" required>
                        </label>
                            </div>  
                            <div>
                            <label for="status"></label>
                            <input type="text" id="status" name="status" required maxlength="1" placeholder="Status(1- OK, 2- NOK, 3-RET, 4-DENIED, 5-NAL)">
                        <div class="error" id="status_error"></div>
                    </div>
                <div>
                    <div>
                        <label for="trans_ref"></label>
                        <input type="text" id="trans_ref" name="rrn" required maxlength="12" placeholder="trans_ref">
                        <div class="error" id="trans_ref"></div>
                    </div>
<!--                     
                        <label for="Tags"></label>
                        <select name="tags" id="tags" required>
                            <option value="1">1-OK</option>
                            <option value="2">2-NOK</option>
                            <option value="3">3-RET</option>
                            <option value="4">4-DENIED</option>
                            <option value="5">3-NAL</option>
                        </select>
                                             -->
            <label for="pdo_ref"><b></b>
        <input type="text" name="reference" id="pdo_ref" required
    maxlength="11" placeholder="PDO_Reference" minlength="10">
        </label></div>
            <div class="login_button"><br>
                <button class="button" type="submit">Post</button>
                    </div><br><br> <br>
                        </div>
                            </div>
                        </div>
                    </form>
                </div>
            <%- include('../partials/scripts'); %> 
        <script>
            document.getElementById('uploadForm').addEventListener('submit', function(event) {
                let isValid = true;
                
                // Clear previous errors
                document.querySelectorAll('.error').forEach(function(errorElement) {
                    errorElement.textContent = '';
                });
         
          
                // Terminal ID validation
                const terminalId = document.getElementById('terminal_id').value;
                if (terminalId.trim() === '') {
                    document.getElementById('terminal_id_error').textContent = 'Terminal ID is required.';
                    isValid = false;
                }
          
                // Status validation
                const status = document.getElementById('status').value;
                if (status.trim() === '' || !['1', '2', '3', '4', '5'].includes(status.trim())) {
                    document.getElementById('status_error').textContent = 'Status must be one of 1, 2, 3, 4, 5.';
                    isValid = false;
                }
          
                // Reference validation
                const reference = document.getElementById('reference').value;
                if (reference.trim() === '') {
                    document.getElementById('reference_error').textContent = 'PDO Reference is required.';
                    isValid = false;
                }
          
                // Image file validation
                const fileInput = document.getElementById('fileInput').files[0];
                if (!fileInput) {
                    document.getElementById('fileInput_error').textContent = 'Image upload is required.';
                    isValid = false;
                } else if (fileInput.size > 5 * 1024 * 1024) { // 5 MB limit
                    document.getElementById('fileInput_error').textContent = 'File size must be less than 5MB.';
                    isValid = false;
                }
          
                if (!isValid) {
                    event.preventDefault(); // Prevent form submission if validation fails
                }
            });
          </script>
          
    </body>
</html>