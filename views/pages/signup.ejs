<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>tetofafrica - Sign Up</title>
  <style>
    * { margin:0; padding:0; box-sizing:border-box; font-family:sans-serif; }
    body { background:#f5f5f5; }
    .wrapper {
      display:flex;
      min-height:100vh;
    }
    .left, .right {
      flex:1;
      padding:60px 40px;
    }
    .left {
      background:#fff;
      display:flex;
      flex-direction:column;
      justify-content:center;
    }
    .right {
      background:linear-gradient(160deg, #133950 0%, #19ddc6 100%);
      color:#fff;
      display:flex;
      flex-direction:column;
      justify-content:center;
      align-items:center;
      text-align:center;
    }
    .right h2 { margin-bottom:10px; }
    .right p { margin-bottom:30px; max-width:300px; }
    .right a {
      color:#fff;
      font-weight:bold;
      text-decoration:underline;
    }
    form {
      max-width:400px;
      margin:auto;
    }
    form h3 {
      margin-bottom:20px;
      font-size:24px;
      color:#333;
      text-align:center;
    }
    .alert_error { color:#d9534f; margin-bottom:10px; text-align:center; }
    .alert_success { color:#5cb85c; margin-bottom:10px; text-align:center; }
    .form-row {
      display:flex;
      gap:20px;
    }
    .form-group {
      margin-bottom:15px;
      width:100%;
    }
    .form-group input {
      width:100%;
      padding:10px;
      border:1px solid #ccc;
      border-radius:4px;
    }
    button {
      width:100%;
      padding:12px;
      background:orangered;
      color:#fff;
      font-size:16px;
      border:none;
      border-radius:5px;
      cursor:pointer;
      transition:background 0.2s ease, transform 0.2s ease;
    }
    button:hover {
      background:rgb(235, 72, 13);
      transform:scale(1.02);
    }
    @media (max-width:768px) {
      .wrapper { flex-direction:column; }
      .right { padding:40px 20px; }
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <!-- Left: Sign-Up Form -->
    <div class="left">
      <form action="/signup" method="POST">
        <h3>Create Your Crata Account</h3>

        <% if (message) { %>
          <div class="alert_error"><%= message %></div>
        <% } %>
        <% if (message_success) { %>
          <div class="alert_success"><%= message_success %></div>
        <% } %>

        <div class="form-row">
          <div class="form-group">
            <input type="text" name="firstname" required placeholder="First Name" maxlength="20" />
          </div>
          <div class="form-group">
            <input type="text" name="lastname" required placeholder="Last Name" maxlength="20" />
          </div>
        </div>
        <div class="form-group">
          <input type="text" name="location" required placeholder="Location" maxlength="25" />
        </div>
        <div class="form-group">
          <input type="tel" name="mobile_num" required placeholder="Mobile Number" maxlength="11" />
        </div>
        <div class="form-group">
          <input type="email" name="email" required placeholder="Email Address" maxlength="64" />
        </div>
        <div class="form-group">
          <input type="text" name="dispatcher_id" required placeholder="NIN / ID Number" maxlength="11" />
        </div>
        <div class="form-row">
          <div class="form-group">
            <input type="password" name="password" required placeholder="Password" minlength="4" />
          </div>
          <div class="form-group">
            <input type="password" name="confirm_password" required placeholder="Confirm Password" minlength="4" />
          </div>
        </div>
        <button type="submit">Begin Your Journey</button>
      </form>
    </div>

    <!-- Right: Branding / Info Panel -->
    <div class="right">
      <h2>Welcome to Crata</h2>
      <p>Join thousands of users delivering with ease and confidence. Let's move forward together.</p>
      <a href="/login">Already have an account? Log In</a>
    </div>
  </div>
</body>
</html>
