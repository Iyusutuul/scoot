<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Crata - Login Page</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f5f5;
      color: #333;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    #header {
      background: linear-gradient(160deg, #133950 0%, #19ddc6 100%);
      padding: 40px 0;
      text-align: center;
      color: white;
      font-weight: 700;
      font-size: 2.8rem;
      letter-spacing: 2px;
      user-select: none;
    }

    nav {
      margin: 20px auto;
      text-align: center;
    }

    nav ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    nav ul li {
      display: inline-block;
      margin: 0 15px;
    }

    nav ul li a {
      text-decoration: none;
      font-weight: 600;
      color: #19ddc6;
      transition: color 0.3s ease;
    }

    nav ul li a:hover {
      color: #eb4810; /* orange hover */
    }

    .alert_error, .alert_success {
      max-width: 400px;
      margin: 0 auto 15px auto;
      padding: 12px 20px;
      border-radius: 5px;
      font-weight: 600;
      text-align: center;
      width: 90%;
    }

    .alert_error {
      background-color: #f8d7da;
      color: #842029;
      border: 1px solid #f5c2c7;
    }

    .alert_success {
      background-color: #d1e7dd;
      color: #0f5132;
      border: 1px solid #badbcc;
    }

    form {
      max-width: 400px;
      background: white;
      margin: 0 auto 50px auto;
      padding: 40px 30px;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgb(0 0 0 / 0.1);
    }

    form h3 {
      margin-bottom: 30px;
      color: #133950;
      font-weight: 700;
      font-size: 1.8rem;
      text-align: center;
    }

    label {
      display: block;
      margin-bottom: 12px;
      font-weight: 600;
      font-size: 0.9rem;
      color: #555;
    }

    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 12px 14px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
      transition: border-color 0.3s ease;
    }

    input[type="email"]:focus,
    input[type="password"]:focus {
      border-color: #19ddc6;
      outline: none;
      box-shadow: 0 0 5px #19ddc6;
    }

    .login_button input[type="submit"] {
      width: 100%;
      padding: 15px 0;
      background-color: #eb4810; /* orange */
      border: none;
      border-radius: 20px;
      color: white;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .login_button input[type="submit"]:hover {
      background-color: #d94e0a;
      transform: scale(1.05);
    }

    .footer-text {
      margin-top: -10px;
      font-size: 85%;
      color: #19ddc6;
      text-align: center;
      font-weight: 600;
    }

    .footer-text a {
      color: #eb4810;
      text-decoration: none;
      font-weight: 700;
    }

    .footer-text a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div id="header">
    Crata
  </div>
  <nav>
    <ul>
      <li><a href="/signup">Sign Up</a></li>
      <li><a href="/supervisor">Admin</a></li>
    </ul>
  </nav>

  <div>
    <% if (message) { %>
      <div class="alert_error"><%= message %></div>
    <% } %>
  </div>

  <form action="/dashboard" method="POST">
    <h3>Login to your account</h3>
    <label for="email">Email</label>
    <input type="email" id="email" name="email" maxlength="64" placeholder="Email" required />

    <label for="password">Password</label>
    <input
      type="password"
      id="password"
      name="password"
      minlength="4"
      maxlength="20"
      placeholder="Password"
      required
    />

    <div class="login_button">
      <input type="submit" value="Login" />
    </div>
  </form>

  <div class="footer-text">
    Don't have an account? <a href="/signup">Click Sign Up!</a>
  </div>

  <%- include('../partials/scripts'); %>

  <script>
    $(document).ready(function () {
      setTimeout(function () {
        $('.alert_error').fadeOut('slow');
        $('.alert_success').fadeOut('slow');
      }, 300000); // 5 minutes
    });
  </script>
</body>
</html>
